#include <stdio.h>

int main()
{

     float PI = 3.14;
    float redius ;

    printf("the perimeter value is ");
    scanf ("%f",&redius);
float result=2*PI*redius;
    printf("the multiples is %f",result);

    return 0;
}